<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" /> <!-- Белый фон -->
    <corners android:radius="12dp" /> <!-- Скругление углов (соответствует MediumComponent) -->
    <stroke
        android:width="1dp"
        android:color="@color/colorOutlineLight" /> <!-- Тонкая обводка -->
</shape>